<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>gnu.io.rxtx</groupId>
    <artifactId>rxtx-parent</artifactId>
    <packaging>pom</packaging>
    <version>2.2-stabilize-SNAPSHOT</version>
    <name>RXTX parent POM</name>
    <url>http://rxtx.qbang.org</url>
    <modules>
        <module>rxtx-java</module>
        <module>rxtx-native</module>
        <module>packaging</module>
    </modules>
    <profiles>
        <profile>
            <id>with-linux-i386</id>
            <activation>
                <os>
                    <family>unix</family>
                    <arch>i386</arch>
                </os>
            </activation>
            <modules>
                <module>native-linux-i386</module>
            </modules>
        </profile>
        <profile>
            <id>with-linux-x86_64</id>
            <activation>
                <os>
                    <family>unix</family>
                    <arch>x86_64</arch>
                </os>
            </activation>
            <modules>
                <module>native-linux-x86_64</module>
            </modules>
        </profile>
        <profile>
            <id>with-windows-i386</id>
            <activation>
                <os>
                    <family>windows</family>
                    <arch>i386</arch>
                </os>
            </activation>
            <modules>
                <module>native-windows-i386</module>
            </modules>
        </profile>
        <profile>
            <id>with-windows-x86_64</id>
            <activation>
                <os>
                    <family>windows</family>
                    <arch>x86_64</arch>
                </os>
            </activation>
            <modules>
                <module>native-windows-x86_64</module>
            </modules>
        </profile>
        <profile>
            <id>failOnUnsupportedPlatforms</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build> 
                <plugins> 
                    <plugin> 
                        <groupId>org.apache.maven.plugins</groupId> 
                        <artifactId>maven-enforcer-plugin</artifactId> 
                        <version>1.1.1</version> 
                        <executions> 
                            <execution> 
                                <id>enforce</id> 
                                <goals> 
                                    <goal>enforce</goal> 
                                </goals>
                                <configuration> 
                                    <rules> 
                                        <AlwaysFail>
                                            <message>Your platform (=os/arch combination) is currently unsupported to build its own binary, however it might be possible to use other platforms to compile the necessary binary for you. This message does not necessarily mean that there is no way to run rxtx on your platform.</message>
                                        </AlwaysFail>
                                    </rules> 
                                    <fail>true</fail> 
                                </configuration> 
                            </execution> 
                        </executions> 
                    </plugin> 
                </plugins> 
            </build>
        </profile>
    </profiles>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>2.3</version>
                    <configuration>
                        <descriptors>
                            <descriptor>src/assemble/jar.xml</descriptor>
                        </descriptors>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>    
</project>
